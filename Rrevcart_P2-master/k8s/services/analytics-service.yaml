apiVersion: apps/v1
kind: Deployment
metadata:
  name: analytics-service
  namespace: revcart
spec:
  replicas: 1
  selector:
    matchLabels:
      app: analytics-service
  template:
    metadata:
      labels:
        app: analytics-service
    spec:
      containers:
        - name: analytics-service
          image: <YOUR_REGISTRY>/revcart/analytics-service:latest
          ports:
            - containerPort: 8089
          envFrom:
            - configMapRef:
                name: revcart-config
          env:
            - name: SPRING_DATA_MONGODB_DATABASE
              value: analytics_db
            - name: SPRING_DATA_MONGODB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: revcart-secrets
                  key: mongo-root-password
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8089
            initialDelaySeconds: 40
            periodSeconds: 15
            failureThreshold: 5
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: 1000m
              memory: 1Gi
---
apiVersion: v1
kind: Service
metadata:
  name: analytics-service
  namespace: revcart
spec:
  selector:
    app: analytics-service
  ports:
    - port: 8089
      targetPort: 8089
