server.port=8086
spring.application.name=payment-service

# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/revcart_payments?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Consul Configuration
spring.cloud.consul.host=localhost
spring.cloud.consul.port=8500
spring.cloud.consul.discovery.service-name=payment-service
spring.cloud.consul.discovery.health-check-path=/actuator/health
spring.cloud.consul.discovery.health-check-interval=10s

# JWT Configuration
jwt.secret=revCartSecretKeyForJWTTokenGenerationAndValidation2024
jwt.expiration=86400000

# Razorpay Configuration
razorpay.key-id=rzp_test_RomyyL3Vtk2P5y
razorpay.key-secret=icGFYdVsLVDyv0GtD1f7o1TS

# Stripe Configuration
stripe.api-key=your-stripe-api-key
stripe.webhook-secret=your-stripe-webhook-secret

# Management endpoints
management.endpoints.web.exposure.include=health,info,consul